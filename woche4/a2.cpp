#include <bits/stdc++.h>

using namespace std;

using ll = long long;

void run() {

    ll n;

    cin >> n;

    ll input[n];
    // ll tmpOutPutIndex[n];
    // ll outPutIndex[n];

    for(int i = 0; i < n; i++) {

        cin >> input[i];
        // outPutIndex[i] = -1;
        // tmpOutPutIndex[i] = -1;
    }

    ll curIndex = 0;

    for(int i = 0; i < n; i++) {

        if(input[i]+1 != input[i+1]){}
    }
}

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    run();
}
