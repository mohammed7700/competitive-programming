#include <bits/stdc++.h>

using namespace std;

using ll = long long;

int is_odd(int x) { return x & 1; }

void run() {
    int* count = 0;

    //red n * 2
    //blue n - 1

    ll count=0, n, m;cin>>n>>m;
}

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    run();
}
